//import { signOut } from '@/auth';

export function SignOut() {
  return null;
  /* return (
   <form
      action={async () => {
        'use server';
        await signOut();
      }}
    >
      <button type="submit">Abmelden</button>
    </form> 
  );*/
}
